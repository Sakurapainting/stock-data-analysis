# 数据库设计文档

## 1. 数据库名称
`iot_stock_hw1`

## 2. 核心数据表
表名：`stock_daily`

## 3. 表结构说明

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT` | 自增主键 |
| `ts_code` | `VARCHAR(16)` | Tushare 股票代码，例如 `600100.SH` |
| `trade_date` | `DATE` | 交易日期 |
| `open_price` | `DECIMAL(12,4)` | 开盘价 |
| `high_price` | `DECIMAL(12,4)` | 最高价 |
| `low_price` | `DECIMAL(12,4)` | 最低价 |
| `close_price` | `DECIMAL(12,4)` | 收盘价 |
| `pre_close` | `DECIMAL(12,4)` | 昨收价 |
| `price_change` | `DECIMAL(12,4)` | 涨跌额 |
| `pct_chg` | `DECIMAL(12,4)` | 涨跌幅 |
| `vol` | `DECIMAL(20,4)` | 成交量 |
| `amount` | `DECIMAL(20,4)` | 成交额 |
| `created_at` | `DATETIME` | 首次写入时间 |
| `updated_at` | `DATETIME` | 最后更新时间 |

## 4. 约束设计
- 主键：`id`
- 唯一键：`uk_ts_code_trade_date (ts_code, trade_date)`
- 说明：唯一键用于支撑增量更新，避免重复存储同一天的数据。

## 5. 设计理由
- `ts_code + trade_date` 可以唯一标识某一支股票在某一天的交易记录。
- 价格、成交量、成交额采用定点数，便于避免浮点误差。
- `created_at` 和 `updated_at` 便于记录数据入库时间，支持后续审计和排错。

## 6. 增量更新机制
- 程序先查询该股票最新的 `trade_date`。
- 若数据库中已存在数据，则从“最新交易日 + 1 天”开始请求 Tushare。
- 入库时采用 `INSERT ... ON DUPLICATE KEY UPDATE`，避免重复写入。

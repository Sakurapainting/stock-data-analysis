# 运行说明

## 1. 环境要求
- Python 3.8 及以上
- MySQL 5.7 及以上
- 依赖库：`numpy`、`pandas`、`matplotlib`、`PyMySQL`、`tushare`、`pylint`
- 如果 MySQL 账号使用 `caching_sha2_password` 或 `sha256_password` 认证方式，还需要 `cryptography`

## 2. 配置步骤
1. 将 `config.ini.example` 复制为 `config.ini`。
2. 在 `config.ini` 中填写以下信息：
   - `tushare.token`：你本人注册 Tushare 后获取的 token
   - `mysql.password`：你本机 MySQL 的密码
   - `stock.student_id`：你的学号
   - `stock.market`：若按沪市规则生成填 `sh`，按深市规则生成填 `sz`
3. 如果学号自动映射后的股票代码没有数据，或老师允许你更换标的，可填写 `stock.ts_code_override` 强制指定股票代码。

## 3. 安装依赖

```powershell
.\setup_homework_env.ps1
```

## 4. 运行主程序

```powershell
# 管理员
net start MySQL80

python main.py
```

如果你已经拉取过数据，只想复用数据库中的历史数据重新生成图表和报告，可以执行：

```powershell
python main.py --skip-fetch
```

如果想先检查 `Tushare token`、积分到期信息和 `daily` 接口权限，可以执行：

```powershell
python check_tushare_access.py
```

## 5. 输出结果
程序运行后会在 `output` 目录生成以下内容：
- `output/data/raw_daily_from_tushare.csv`：Tushare 原始日线数据
- `output/data/processed_daily.csv`：预处理后的日线数据
- `output/data/weekly_agg.csv`：周度聚合数据
- `output/data/monthly_agg.csv`：月度聚合数据
- `output/data/yearly_agg.csv`：年度聚合数据
- `output/figures/*.png`：周、月、年分析图表
- `output/report.md`：综合分析报告

## 6. 代码结构
- `main.py`：程序入口
- `hw1_stock_analysis/config_loader.py`：配置加载与校验
- `hw1_stock_analysis/stock_code.py`：学号到股票代码映射
- `hw1_stock_analysis/db.py`：MySQL 初始化与数据读写
- `hw1_stock_analysis/fetch_store.py`：Tushare 抓取与增量入库
- `hw1_stock_analysis/preprocess.py`：数据清洗与特征工程
- `hw1_stock_analysis/weekly_analysis.py`：周度分析
- `hw1_stock_analysis/monthly_analysis.py`：月度分析
- `hw1_stock_analysis/yearly_analysis.py`：年度分析
- `hw1_stock_analysis/report_generator.py`：报告生成

## 7. Pylint 检查
```powershell
python -m pylint main.py hw1_stock_analysis
```

## 8. 提交建议
- 提交代码目录时保留 `config.ini.example`，不要提交你自己的真实 token 和数据库密码。
- 运行一次完整流程后，把 `output` 目录中的 CSV、图表和报告一并提交。

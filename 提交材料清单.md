# 提交材料清单

本目录已包含课程作业提交所需的 5 类材料，具体对应如下。

## 1. 完整源代码（.py 文件）

- `main.py`：主程序入口
- `check_tushare_access.py`：Tushare token 与接口权限检查脚本
- `hw1_stock_analysis/*.py`：数据抓取、数据库读写、数据预处理、周/月/年分析、报告生成等完整模块代码

## 2. 数据库设计文档

- `数据库设计文档.md`

文档内容包含数据库名称、核心数据表、表结构、字段类型、字段说明、主键、唯一键和增量更新机制。

## 3. 分析报告

- `output/report.md`

报告内容包含基本信息、数据预处理结论、周度分析、月度分析、年度分析、综合结论和投资建议。

报告引用的图表文件位于：

- `output/figures/weekly_candlestick.png`
- `output/figures/weekly_return_bar.png`
- `output/figures/monthly_close_line.png`
- `output/figures/monthly_volume_heatmap.png`
- `output/figures/annual_return_bar.png`
- `output/figures/cumulative_return_curve.png`

## 4. 数据文件（CSV 格式）

- `output/data/raw_daily_from_tushare.csv`：Tushare 原始日线数据
- `output/data/processed_daily.csv`：预处理后的日线数据
- `output/data/weekly_agg.csv`：周度聚合数据
- `output/data/monthly_agg.csv`：月度聚合数据
- `output/data/yearly_agg.csv`：年度聚合数据

## 5. 运行说明文档

- `运行说明.md`

文档内容包含环境要求、配置步骤、依赖安装、主程序运行命令、输出结果说明、代码结构和 Pylint 检查命令。

## 验证记录

- 已执行 `python -m compileall main.py hw1_stock_analysis`，源码语法检查通过。
- 已确认 `output/data` 目录包含原始数据和处理后数据 CSV。
- 已确认 `output/figures` 目录包含报告所需图表。
